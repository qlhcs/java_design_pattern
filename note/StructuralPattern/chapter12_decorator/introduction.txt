关键词：
    聚合替代继承
适用场景：
    动态增加一个对象的职责；（动态 -- 聚合）
    避免大量子类的产生（二维角度看，通过继承的方式将产生大量子类）
优点：
    【装饰模式比继承更加灵活】，不会导致子类急剧增加
    具体装饰类和被装饰类【独立变化】，互不影响，符合开闭原则
缺点：
    1、外观类不完全符合开闭原则
    2、限制客户端对子系统的直接访问，影响系统的灵活性和可变性
个人理解：
    组合模式的变体（区别在于抽象装饰类），且达到了桥接模式的效果（每个维度独立变化 -- 装饰类和被装饰类独立变化）
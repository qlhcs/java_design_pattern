适用场景/优点：
    某个类全局只有一个实例，只提供访问的权限。

常见问题：
    1、饿汉式单例的线程安全问题及解决方案（synchronized、volatile）
    2、懒汉式单例和饿汉式单例的对比角度：内存分配时机、线程安全性 + 性能（dcl加锁影响性能）
    3、（了解）懒汉式单例的其它实现方式 -- Java语言特性 使用静态内部类创建实例 JVM能保证线程安全，不需要加锁
相关概念：
    浅克隆：克隆的对象独立分配内存地址，其成员变量仅复制原内存地址
    深克隆：克隆的对象独立分配内存地址，其成员变量也独立分配内存地址
    Java实现浅克隆：克隆类 实现Cloneable接口
    Java实现深克隆：克隆类 和 成员变量中所有引用类 实现Serializable接口

适用场景/优点：
    创建对象成本较大，导致系统资源消耗大（如CPU占用资源多；RPC远程调用超时），可以考虑用原型模式减少创建对象的成本

缺点：
    实现深克隆，维护类的时候需要修改比较多的类（要考虑所有引用类）
